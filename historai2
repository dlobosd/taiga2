<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Sesión - HU #7</title>
    <style>
        /* Estilos CSS básicos */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }

        .contenedor-login {
            background-color: #ffffff;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 350px;
            text-align: center;
        }

        h2 {
            color: #333;
            margin-bottom: 25px;
        }

        /* Estilo para el botón de Google (lo más importante de esta HU) */
        .boton-google {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ddd;
            border-radius: 4px;
            background-color: #4285F4; /* Color de Google */
            color: white;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }

        .boton-google:hover {
            background-color: #357ae8;
        }

        .icono-google {
            margin-right: 10px;
            /* Usar una imagen o un SVG real sería mejor, aquí solo es un placeholder */
            font-size: 1.2em; 
        }

        .separador {
            margin: 20px 0;
            display: flex;
            align-items: center;
            color: #aaa;
            font-size: 14px;
        }

        .separador::before, .separador::after {
            content: '';
            flex-grow: 1;
            height: 1px;
            background: #eee;
            margin: 0 10px;
        }

        /* Estilos para el formulario tradicional (opcional) */
        input[type="email"],
        input[type="password"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        .boton-login-tradicional {
            width: 100%;
            padding: 10px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="contenedor-login">
        <h2>Iniciar Sesión</h2>

        <a href="#" class="boton-google" role="button">
            <span class="icono-google">G</span>
            Continuar con Google
        </a>

        <div class="separador">o</div>

        <form action="/api/login" method="POST">
            <input type="email" name="email" placeholder="Correo electrónico" required>
            <input type="password" name="password" placeholder="Contraseña" required>
            <button type="submit" class="boton-login-tradicional">Iniciar Sesión</button>
        </form>

        <p style="margin-top: 20px; font-size: 14px;">
            ¿No tienes cuenta? <a href="registro.html">Regístrate aquí</a>
        </p>
    </div>
</body>
</html>